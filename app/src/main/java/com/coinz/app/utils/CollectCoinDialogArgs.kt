package com.coinz.app.utils

// TODO: This feels like it could be a named companion object of CollectCoinDialogFragment!
/**
 * CollectCoinDialog Arguments.
 *
 * Object holding all argument names used when passing data to the CollectCoinDialogFragment. Makes
 * putting and retrieving easy, as we don't need to give explicit strings anymore but rather just
 * refer to the same constant defined here.
 */
object CollectCoinDialogArgs {

    const val coinId = "coin_id"
    const val markerDist = "marker_dist"

}
